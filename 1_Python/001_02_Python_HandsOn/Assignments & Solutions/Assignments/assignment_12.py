'''
This exercise will require several lines of code. 

Write a script that opens a file named 'test.txt', writees 'Hello World' to the file, then closes it. 

For example, the followinf code opens a file called 'myfile.txt', writes 'this is my file', and closes it: 
	x=open('myfile.txt','w')
	x.write('This is my file')
	x.close()
'''